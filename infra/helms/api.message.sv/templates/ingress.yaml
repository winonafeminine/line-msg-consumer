apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lmc-api-message-ingress
  annotations:
    # kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/use-reges: 'true'
spec:
  rules:
  - host: "line-message-consumer.org"
    http:
      paths:
      - pathType: Prefix
        path: "/message"
        backend:
          service:
            name: lmc-api-message-sv
            port:
              number: 5000