import { generatedApiChatSvTypes } from './generated-api.chat-sv-types';

describe('generatedApiChatSvTypes', () => {
    it('should work', () => {
        expect(generatedApiChatSvTypes()).toEqual(
            'generated-api.chat-sv-types'
        );
    });
});
